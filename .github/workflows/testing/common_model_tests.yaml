name: Common Model Tests

on:
  push:
    branches: [ "master" ]
    paths:
      - src/models/**/common/*
      - src/models/contexts/common/*
      - src/models/dataset/common/*
      - src/steps/**/common/*
  pull_request:
    branches: [ "master" ]
    paths:
      - src/models/**/common/*
      - src/models/contexts/common/*
      - src/models/dataset/common/*
      - src/steps/**/common/*
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  common-model-tests:
    uses: ./.github/workflows/testing/testing_workflow_template.yaml
    with:
      use_lfs: true
      test_paths: |
        tests/models/**/common/*
        tests/models/dataset/common/*
        tests/models/steps/**/common/*
        tests/steps/**/common/*
